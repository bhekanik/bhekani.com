---
import { format } from "date-fns";
import Link from "./Link.astro";
const { title, url, date } = Astro.props;
---

<li>
  <Link href={url}>
    {title}
    <span class="text-sm whitespace-nowrap text-[hsl(var(--muted-foreground))]"
      >{format(date, "MMMM dd, yyyy")}</span>
  </Link>
</li>
