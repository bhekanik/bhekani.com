---
type Props = {
  tag: string
  selected?: boolean
}

const { tag, selected } = Astro.props
---

<a class="tag" aria-current={selected ? "true" : undefined} href={`/posts/tags/${tag}`}>
  #{tag}
</a>
