---
type Props = {
  href: string;
  title: string;
};

const { href, title } = Astro.props;
---

{
  title === "Github" ? (
    <a rel="me" class="text-muted-foreground" href={href}>
      {title}
    </a>
  ) : (
    <a class="text-muted-foreground" href={href}>
      {title}
    </a>
  )
}
